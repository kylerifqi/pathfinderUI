export enum Statuses {
	complete,
	failed,
	continue,
	ready
}

// export enum mouseActions {
// 	createSolid,
// 	removeSolid,
// 	moveStart,
// 	moveEnd
// }

export enum Generators {
	none,
	depthFirst,
	openSimplexNoise
}

export enum PanelControlButtons {
	start,
	stop,
	restart,
	quick,
	grid
}